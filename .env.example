# Frontend (Vercel + local Next)
NEXT_PUBLIC_CONVEX_URL=

# Local Convex CLI only — never put this on Vercel as a preview key if you
# want production. Vercel uses CONVEX_DEPLOY_KEY from the Convex dashboard.
# CONVEX_DEPLOY_KEY=

# Convex dashboard env (not Vercel):
# GROQ_API_KEY=
# JWT_PRIVATE_KEY=
# JWKS=
# DEV_UNLIMITED_GENERATE=true
